sap.ui.define([], function () {
    "use strict";

    return {
        formatVatAmount: function (netPrice,unitDiscount, netDiscount, vatPercent) {
            
            return vatPercent; // Formatting to 2 decimal places
        }
    };
});